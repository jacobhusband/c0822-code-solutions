/* exported setValue */

// use the first two arguments to access the key property of the object and assign it the value passed in which is the third argument

function setValue(obj, key, val) {
  obj[key] = val;
}
