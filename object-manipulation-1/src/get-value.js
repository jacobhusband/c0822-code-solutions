/* exported getValue */

// first argument from the input is obj and second argument is key
// put key in bracket notation of obj and return it

function getValue(object, key) {
  return object[key];
}
